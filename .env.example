# Example environment variables for local and Vercel builds

# Optional: absolute URL (or same-origin path) to the Verge3D app HTML.
# If unset, the app uses the bundled public/ path:
#   /verge3d/CAD platform/CAD platform.html
#
# Same-origin (bundled in this repo, simplest):
# VITE_VERGE_APP_URL=/verge3d/CAD platform/CAD platform.html
#
# External CDN (if assets are large / hosted elsewhere):
# VITE_VERGE_APP_URL=https://cdn.example.com/verge3d/CAD%20platform/CAD%20platform.html